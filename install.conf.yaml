- defaults:
    link:
      create: true
      relink: true
      force: true

- shell:
  - [git submodule update --init --recursive, Installing submodules]

- link:
    ~/.zshrc: .zshrc
    ~/.vimrc: .vimrc
    ~/.vim: vim
    ~/.tmux.conf: .tmux.conf
    ~/.ssh/config: ssh/config
    ~/.ssh/homelab-admin.pub: ssh/homelab-admin.pub
    ~/.ssh/proxmox.pub: ssh/proxmox.pub
    ~/.ssh/raspberrypi.pub: ssh/raspberrypi.pub
    ~/.ssh/truenas.pub: ssh/truenas.pub
    ~/.ssh/openwrt-router.pub: ssh/openwrt-router.pub
    ~/.claude/CLAUDE.md: claude/CLAUDE.md
    ~/.claude/settings.json: claude/settings.json
    ~/.claude/.mcp.json: claude/.mcp.json
    ~/.claude/agents: claude/agents
    ~/.claude/rules: claude/rules
    ~/.claude/skills: agent-skills/skills
    ~/.codex/AGENTS.md: codex/AGENTS.md
    ~/.codex/rules: codex/rules
    ~/.codex/agents: codex/agents
    ~/.agents/skills: agent-skills/skills

- shell:
  - ['mkdir -p .git/hooks && ln -sf ../../codex/hooks/post-merge .git/hooks/post-merge && ln -sf ../../codex/hooks/post-checkout .git/hooks/post-checkout', Installing Codex git hooks]
  - [codex/bin/codex-sync, Syncing Codex portable config]
