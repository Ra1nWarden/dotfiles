- defaults:
    link:
      create: true
      relink: true

- link:
    ~/.zshrc: .zshrc
    ~/.vimrc: .vimrc
    ~/.vim: vim
    ~/.claude: claude

- shell:
  - [git submodule update --init --recursive, Installing vim plugins]
